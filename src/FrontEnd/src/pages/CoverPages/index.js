import { Card, Button } from 'antd'
import './index.scss'

const CoverPages = () => {
  return (
    <div className="login">
        <Button className='but' type="primary" htmlType="submit" size="large" block>
              Click open the world
        </Button>
    </div>
  )
}

export default CoverPages